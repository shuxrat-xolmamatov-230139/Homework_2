#include <iostream>

using namespace std;

int main(){

    int base, height;
    cin >> base >> height;

    cout << "Area of triangle is equal to " << float(base * height) / 2;

    return 0;
}